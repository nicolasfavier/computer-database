package com.nicolas.validator;

import com.nicolas.dto.ComputerDto;
import com.nicolas.utils.Utils;

public class ComputerDtoValidator {

	public static final void validate(ComputerDto computerDto, Error error){
//		// check if dates are valid
//		if (!Utils.isDate(introduced)) {
//			error = true;
//			errorMessage += "date : " + introduced + " doesn't follow the format yyyy/mm/dd  <br/>";
//		}
//
//		if (!Utils.isDate(discontinued)) {
//			error = true;
//			errorMessage += "date : " + discontinued
//					+ " doesn't follow the format yyyy/mm/dd  <br/>";
//		}
//
//		if (error) {
//			request.setAttribute("errorMessage", errorMessage);
//			doGet(request, response);
//			return;
//		}
	}
}
